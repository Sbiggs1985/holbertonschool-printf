#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <stdarg.h>

int _printf(const char *format, ...);
void _putchar(char c);
char c = (char)va_arg(args, int);
char *s = va_arg(args, char *);

#endif
